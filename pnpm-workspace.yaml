# pnpm 10 以降は既定で依存パッケージの postinstall 等をブロックする (サプライチェーン対策)。
# ここで明示的に許可したものだけがスクリプトを実行できる。
#   esbuild        : Vite の中核。ネイティブバイナリの配置に postinstall が必要
#   @parcel/watcher: watch 用のオプショナル依存。無くても動くため拒否する
allowBuilds:
  '@parcel/watcher': false
  esbuild: true
